version: '3.8'

services:
  ziya-voice:
    image: aspirentech/ziya-voice:latest
    container_name: ziya-voice
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
